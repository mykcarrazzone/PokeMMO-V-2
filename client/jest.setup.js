import "@testing-library/jest-dom/extend-expect";
// Importing the jest-dom library for additional expect functions

jest.setTimeout(30000); // Set the timeout for async tests to 30 seconds

beforeAll(() => {
  // Add any necessary global setup here
});

afterAll(() => {
  // Add any necessary global teardown here
});
